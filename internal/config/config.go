// Package config manages Hatch application configuration,
// including project definitions and global settings.
package config

// AppName is the canonical name of the application.
const AppName = "hatch"
